# Security

This project is an educational local demo. It has no accounts, payments, or stored user profiles. The Flask development server binds to `127.0.0.1` and should not be exposed directly to the internet.

Do not commit `.env` files, virtual environments, databases, or user-submitted data. Report a security issue privately through the contact method on the repository owner's GitHub profile rather than opening a public issue with sensitive details.

